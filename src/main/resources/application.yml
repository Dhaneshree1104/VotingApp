spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zinterview?serverTimezone=UTC
    username: root
    password: Letmein@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
